{{#paper-toolbar}}
  {{#paper-toolbar-tools}}

    {{#paper-menu offset="-5 50" as |menu|}}
      {{#menu.trigger}}
        {{#paper-button iconButton=true}}
          {{paper-icon "menu"}}
        {{/paper-button}}
      {{/menu.trigger}}

      {{#menu.content width=4 as |content|}}
        {{#content.menu-item}}
          <span>TEST 1</span>
        {{/content.menu-item}}
        {{#content.menu-item}}
          <span>TEST 1</span>
        {{/content.menu-item}}
      {{/menu.content}}
    {{/paper-menu}}

    <h2>
      Fortnite Tools
    </h2>
    <span class="flex"></span>
    {{#paper-button iconButton=true}}
      {{paper-icon "favorite"}}
    {{/paper-button}}
    {{#paper-button iconButton=true}}
      {{paper-icon "more_vert"}}
    {{/paper-button}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}}

{{outlet}}
