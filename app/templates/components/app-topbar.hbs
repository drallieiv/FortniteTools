{{#paper-toolbar}}
  {{#paper-toolbar-tools}}
    {{#paper-button iconButton=true onClick=(transition-to "index")}}
      {{paper-icon "home"}}
    {{/paper-button}}
    {{#link-to "index"}}
      <h2>Fortnite Tools</h2>
    {{/link-to}}
    {{#if activeTool}}
      {{paper-icon "chevron-right"}}
      <h3>{{activeTool}}</h3>
    {{/if}}
    <span class="flex"></span>
    {{#paper-button iconButton=true}}
      {{paper-icon "favorite"}}
    {{/paper-button}}
    {{#paper-button iconButton=true}}
      {{paper-icon "more_vert"}}
    {{/paper-button}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}}
